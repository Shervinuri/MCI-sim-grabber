[build]
  # مسیر فایل‌های سایت شما
  publish = "public"
  # مسیر فانکشن‌های شما
  functions = "netlify/functions"

# این بخش برای دسترسی راحت‌تر به فانکشن است
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
